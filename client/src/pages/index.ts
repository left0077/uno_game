/**
 * Pages Layer - 页面层统一导出
 * 
 * 页面层职责：
 * - 只负责 UI 渲染
 * - 接收数据和回调作为 props
 * - 不直接操作 Socket 或状态管理
 */

export { HomePage } from './HomePage';
export { RoomPage } from './RoomPage';
export { GamePage } from './GamePage';
